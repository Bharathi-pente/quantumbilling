// Placeholder e2e test — passes trivially; real tests added in D-01+
describe('AppController (e2e)', () => {
  it('/health (GET)', () => {
    // Real e2e tests require Keycloak + Postgres; placeholder for now
    expect(true).toBe(true);
  });
});
